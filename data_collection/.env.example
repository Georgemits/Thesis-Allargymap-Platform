# Optional: only needed if you run data_collection/ scripts standalone (outside
# the backend/Docker context) and don't want to rely on the backend/.env fallback.
# Copy this to data_collection/.env and fill in real values; never commit .env.

GOOGLE_POLLEN_API_KEY=your-google-pollen-api-key-here
MONGO_URI=mongodb://localhost:27017/allergymap
